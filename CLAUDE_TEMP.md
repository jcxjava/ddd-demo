## DDD-Java 后端开发规范

> **语言要求**：所有沟通、方案、注释统一使用中文。

---

### 1. 技术栈

| 技术 | 版本 | 用途 |
| --- | --- | --- |
| Java / Spring Boot | 17 / 3.5.14 | 运行环境 / Web、Validation、事件发布 |
| MyBatis-Plus / MySQL | 3.5.16 / 8.0+ | ORM / 关系型数据库 |
| MapStruct / SpringDoc | 1.6.3 / 2.8.13 | 对象转换 / API 文档 |
| Lombok / JUnit 5 / Mockito | 继承 parent | 简化代码 / 单元测试 |

**禁止私自修改版本。**

---

### 2. DDD 四层架构

`接口层(api) → 应用层(application) → 领域层(domain) → 基础设施层(infrastructure)`

| 层 | 职责 | 禁止 |
| --- | --- | --- |
| **接口层** | 接收请求、`@Valid` 校验、返回 `ApiResponse`、调用应用层 | 业务逻辑、直接调 Repository |
| **应用层** | 流程编排、`@Transactional` 事务、事件发布 | 核心业务规则(注)、直接操作数据库 |
| **领域层** | 实体行为（充血模型）、业务规则、领域服务、值对象、仓储接口 | 依赖 Spring/MyBatis 注解、SQL |
| **基础设施层** | 数据库 CRUD、外部集成、工具类、转换实现 | 业务逻辑、将 DO/PO 暴露给上层 |

> **注**：核心业务规则判定标准——若一段逻辑在多个应用服务中复用，或独立于流程编排仍有业务含义，就属于领域规则，应放在领域层。

**分层测试策略**：领域层先写纯单测（不启动 Spring，Mock 仓储），应用层 Mock 仓储单测，基础设施层后补集成测试（真实 DB），接口层后补 MockMvc。
覆盖要求：领域 ≥ 90% / 应用 ≥ 80% / 基础设施 ≥ 70% / 接口 ≥ 60%。命名 `{被测类}Test`，Given-When-Then 三段式。

---

### 3. 目录结构

```
{basePackage}/
├── api/                            # 接口层 (Api)
│   ├── controller/                 # REST 控制器
│   └── dto/request/, response/      # 接口 DTO，含校验注解
├── application/                    # 应用层 (Application)
│   ├── service/                    # 应用服务，流程编排 + @Transactional
│   ├── command/                    # 命令对象（写操作入参）
│   ├── converter/                  # DTO ↔ Command 转换（MapStruct）
│   └── event/                      # 领域事件 + listener/ 监听器
├── domain/                         # 领域层 (Domain)，不依赖任何框架
│   └── {bounded-context}/          # 按限界上下文分包（order/、product/ 等）
│       ├── model/
│       │   ├── entity/             # 实体：有 ID，可变，充血模型
│       │   ├── valueobject/        # 值对象：无 ID，不可变
│       │   └── enums/              # 领域枚举
│       ├── service/                # 领域服务：跨实体的业务规则
│       └── repository/             # 仓储接口：仅契约，不含实现
├── infrastructure/                 # 基础设施层 (Infrastructure)
│   ├── config/                     # Spring 配置
│   ├── mapper/                     # MyBatis-Plus BaseMapper
│   ├── persistence/po/             # 持久化对象（DO/PO），表映射
│   ├── converter/                  # Entity ↔ PO 转换（MapStruct）
│   └── repository/impl/            # 仓储实现
├── common/                         # 全局公共组件
│   ├── enums/                      # ResponseCode 等
│   ├── exception/                  # BusinessException
│   ├── handler/                    # GlobalExceptionHandler
│   └── model/api/                  # ApiResponse、PageResult
└── resources/
    ├── application.yml             # 唯一配置文件
    └── db/schema.sql, data.sql
```

---

### 4. 编码规范

#### 4.1 对象分离

| 对象 | 位置 | 职责 |
| --- | --- | --- |
| DO / PO | `infrastructure/persistence/po/` | 数据库表映射（`@TableName` / `@TableId`） |
| 领域实体 | `domain/{bounded-context}/model/entity/` | 充血模型，含业务行为 |
| 值对象 | `domain/{bounded-context}/model/valueobject/` | 无 ID，不可变 |
| DTO | `api/dto/` | 接口交互，含校验注解 |
| Command | `application/command/` | 应用层入参（内部类如 `OrderItemCommand`） |

> **严禁混用**：接口层不暴露领域实体，DO/PO 不传出基础设施层。Controller 返回 DTO，不返回实体。

#### 4.2 统一响应与异常

- `ApiResponse<T>` 统一包装为 `{"code":"200","message":"success","data":{}}`，`PageResult<T>` 分页封装
- `BusinessException` 业务异常，携带错误码
- `@RestControllerAdvice` 全局捕获

**错误码分段**（统一放在 `common/enums/ResponseCode`）：

| 段 | 范围 | 说明 |
| --- | --- | --- |
| 成功 | 200 | 请求成功 |
| 框架级 | 1000-1099 | 1001 参数校验失败、1002 系统未知异常 |
| 通用业务 | 1100-1199 | 1100 资源不存在、1101 重复操作/幂等冲突、1102 状态不允许、1103 权限不足 |
| 业务域 | 1200+ | 按域分段：1200-1299 用户、1300-1399 订单、1400-1499 商品、1500-1599 支付 |

#### 4.3 注释规范

- **语言**：全部使用中文
- **类/接口**：必须写 JavaDoc，说明职责和核心行为
- **公共方法**：必须写 JavaDoc，说明意图、关键参数、副作用（如并发约束）
- **字段**：仅当含义无法从命名直接推断时，用单行 `/** */` 补充说明
- **无需注释**：getter/setter、显而易见的赋值、纯转发的委托方法

#### 4.4 日志规范

- **框架**：使用 `@Slf4j`（Lombok），禁止 `System.out` / `e.printStackTrace()`
- **占位符**：一律用 `{}`，禁止字符串拼接
- **语言**：日志内容使用中文

| 级别 | 使用场景 | 示例 |
| --- | --- | --- |
| `info` | 关键业务节点（状态变更、定时任务起止、外部调用） | `log.info("订单 {} 已创建，用户：{}", orderId, userId)` |
| `warn` | 可恢复的异常或降级场景（参数越界、缓存穿透、配额耗尽） | `log.warn("积分余额不足，会员：{}，需扣减：{}", memberId, amount)` |
| `error` | 系统异常、不可恢复的错误，携带上下文信息 | `log.error("积分过期定时任务执行失败", ex)` |

- **禁止**：循环中打日志（如需打印集合内容，先收集再一次性输出）；打印敏感信息（密码、手机号、身份证）

---

### 5. DDD 开发流程

所有大型功能均按 DDD 思路分析、设计、开发。开发前先根据需求复杂度判定路径：

| 维度 | 快速改动 | 大型功能 |
| --- | --- | --- |
| 影响范围 | 单文件 / 单方法 / 单接口 | 跨表 / 跨模块 / 新业务流程 |
| 数据表 | 不改表或仅加字段 | 新增表或改关联关系 |
| 接口数量 | 单个接口微调 | 多接口联动或新增一组接口 |
| 业务复杂度 | 参数校验 / 常量枚举 / 局部规则 | 新业务域 / 核心流程变更 |

**判定规则**：命中「大型功能」任一维度即走大型流程；全部落在「快速改动」则走快速策略。

#### 5.1 快速改动

1. 仅针对目标位置开发，不扩散无关代码
2. 单次完成编码、格式化、自测，无需全局方案
3. 同步补充单元测试
4. 完工后统一告知成果

#### 5.2 大型功能

##### 5.2.1 需求分析与方案设计

| 步骤 | 做什么 | 产出 |
| --- | --- | --- |
| **需求分析** | 梳理业务流程、参与者角色、业务目标，区分核心业务/支撑业务/通用能力 | 核心流程、业务分类 |
| **边界设定** | 通过领域事件（发生了什么 → 谁触发 → 谁关心）划定限界上下文与上下游关系 | 限界上下文边界、统一语言词汇表 |
| **领域建模** | 在各上下文内识别聚合根/实体/值对象/领域服务、领域事件 | 领域模型 |
| **方案输出** | 整理核心业务流程、接口契约、领域事件、表结构（由领域模型推导）、事务/幂等/并发要点，写入 `docs/` | 设计文档 |

> 需求分析有疑问需对齐澄清；领域建模完成后**暂停确认**；方案输出后**暂停确认**方可编码。
> **聚合设计约束**：聚合尽量小，通过 ID 引用外部聚合；同一聚合内强一致性，聚合间最终一致性。**领域事件命名**：`{实体}{动作}Event`（如 `OrderPaidEvent`），跨上下文消费在应用层异步监听。

##### 5.2.2 任务拆解与执行

按**聚合**垂直拆解 Task，逐层向下推进。全链路变更走 领域层 → 持久层 → 应用层 → 接口层 → 转换 → 测试；纯领域规则变更可仅覆盖领域层 + 测试。不同聚合可并行推进。不确定的模型可先写领域层 + 单测验证后再补全。

- **关键 Task**（聚合根/核心规则）：完成后暂停确认
- **常规 Task**（CRUD/样板）：批次连续执行，统一汇报

##### 5.2.3 收尾

全部验收通过后，统一核对跨上下文串联。

---

### 6. 迭代变更规范

| 变更类型 | 修改范围 |
| --- | --- |
| 新增功能 | 全层增量开发 |
| 业务规则变更（不涉及新增字段） | 领域层 + 单元测试 |
| 接口字段调整 | DTO + Controller + application/converter |
| 数据库变更 | DDL → DO → Mapper → 实体 → Converter → DTO（如需暴露） |

---

### 7. 通用约束

- 测试目录与 main 目录结构镜像（`src/test/java/{basePackage}/...`）
- 禁止跨层调用（接口层 → 基础设施层）；跨领域访问必须通过应用层，领域层不可直接依赖其他领域的仓储或服务
- 基础设施层的 Repository 实现不得将 Spring/MyBatis 依赖暴露给领域层（领域层仓储接口保持纯净）
- 不引入半成品抽象，不设计假设性功能；优先复用，确认重复模式后再提取公共逻辑
