# 通用编码规范与约束

## 开发流程

### 第一步：需求分析与架构设计
- 梳理核心业务流程、实体关系和业务边界
- 输出明确的分层架构设计和各层职责
- 规划全局统一响应、全局异常、分页等公共组件
- 给出标准化开发顺序

### 第二步：全局编码约束
后续所有代码严格遵守以下约束，禁止私自改动：
- 技术栈固定，不允许随意引入新依赖
- 严格遵守分层规则，禁止跨层调用
- 遵守对象分离原则

### 第三步：原子化分步开发
1. **定义领域实体、业务枚举、核心业务规则**
   - 创建领域实体和值对象
   - 定义业务枚举和状态机
   - 实现核心业务规则

2. **开发仓储接口及数据库实现层**
   - 创建仓储接口
   - 实现仓储实现类
   - 编写数据访问层代码

3. **开发Controller接口，实现参数校验与结果封装**
   - 创建Controller
   - 实现参数校验
   - 统一响应封装

4. **对已生成代码做全量审查与优化**
   - 代码风格检查
   - 业务逻辑验证
   - 性能优化

5. **优化事务逻辑，修补潜在漏洞**
   - 事务边界定义
   - 并发控制
   - 异常处理完善

6. **分阶段开发与验证**
   - 每完成一个功能模块进行检查
   - 确认无误后再继续后续开发
   - 及时修复发现的问题，避免问题累积

## 需求迭代变更规范

### 核心原则
- 严格遵守：固定整体架构、目录结构、全局公共组件不变
- 仅增量修改DTO、领域业务规则、应用层流程
- 复用原有工具、配置、转换逻辑，不重构底层架构

### 迭代策略
1. 新功能开发时，复用现有分层结构
2. 业务规则变更时，只修改领域层相关代码
3. 接口变更时，只调整DTO和Controller
4. 数据库变更时，只修改数据对象和数据访问层

## 分层规则

### 1. 接口层
- **职责**：仅做参数接收、校验、接口响应
- **允许**：接收HTTP请求、参数校验（@Valid）、返回统一响应结构、调用应用层Service
- **禁止**：包含任何业务逻辑、直接调用仓储层、跨Service调用

### 2. 应用层
- **职责**：流程编排、事务控制、能力聚合
- **允许**：协调多个领域服务、管理事务边界、处理用例流程、调用基础设施层
- **禁止**：包含核心业务规则、直接操作数据库

### 3. 领域层
- **职责**：承载核心业务规则、实体、业务枚举，纯业务逻辑
- **允许**：业务规则实现、实体行为定义（充血模型）、领域服务封装、值对象设计
- **禁止**：依赖外部框架、包含技术实现细节、直接依赖数据库

### 4. 基础设施层
- **职责**：仅做数据库操作、工具实现、外部能力适配
- **允许**：数据库CRUD操作、外部系统集成、工具类实现、配置管理
- **禁止**：包含业务逻辑、直接暴露给上层

## 编码规范

### 1. 对象分离原则
- **DO（Data Object）**：数据库表映射对象，仅用于数据持久化
- **领域实体**：包含业务逻辑和行为的对象（充血模型）
- **DTO/VO**：数据传输对象，用于接口层交互
- **严禁混用**：三者职责分离，禁止直接互相替代使用

### 2. 对象转换规范
- **使用MapStruct**进行对象转换，禁止手写getter/setter转换
- **转换位置**：统一在应用层进行转换
- **示例**：
```java
@Mapper
public interface XxxConverter {
    XxxDTO toDTO(Xxx entity);
    Xxx toEntity(XxxDTO dto);
}
```

### 3. RESTful接口规范
- **统一三段式返回结构**：
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {}
  }
  ```
- **HTTP状态码约定**：
  - 200：成功
  - 400：参数错误
  - 401：未授权
  - 403：无权限
  - 404：资源不存在
  - 500：系统错误

### 4. 参数校验规范
- **入参必填校验**：
  ```java
  @NotNull(message = "xxx不能为空")
  private String fieldName;
  ```
- **业务合法性校验**：
  ```java
  @AssertTrue(message = "xxx不合法")
  private boolean isFieldValid();
  ```

### 5. 异常处理规范
- **自定义业务异常**：
  ```java
  public class BusinessException extends RuntimeException {
      private int code;
      private String message;
  }
  ```
- **全局统一异常捕获**：
  ```java
  @RestControllerAdvice
  public class GlobalExceptionHandler {
      @ExceptionHandler(BusinessException.class)
      public ApiResponse<?> handleBusinessException(BusinessException e) {
          return ApiResponse.fail(e.getCode(), e.getMessage());
      }
  }
  ```

### 6. 编码风格
- **充血模型**：业务逻辑封装在领域对象中，应用层只做流程编排
- **策略模式**：同类型多变体行为使用策略模式，通过接口抽象统一调度
- **仓储模式**：领域层定义仓储接口，基础设施层实现，上层只依赖接口

## 项目结构规范

```
src/main/java/{basePackage}/
├── api/                    # 接口层
│   ├── controller/         # 控制器
│   └── dto/               # 数据传输对象
├── application/            # 应用层
│   ├── service/           # 应用服务
│   ├── command/           # 命令对象
│   └── event/             # 事件对象
├── domain/                 # 领域层
│   ├── model/             # 领域模型（实体、值对象、枚举）
│   ├── service/           # 领域服务
│   └── repository/        # 仓储接口
└── infrastructure/        # 基础设施层
    ├── repository/        # 仓储实现
    ├── mapper/            # 数据访问层
    └── config/            # 配置类
```

## 其他约束

1. 禁止私自改动技术栈
2. 严格遵守分层规则，禁止跨层调用
3. 代码需通过静态检查
4. 单元测试覆盖率不低于80%
5. API文档需自动生成
6. 优先复用已有代码，不重复造轮子
7. 领域层不依赖任何框架类（除Lombok等编译期工具外）
